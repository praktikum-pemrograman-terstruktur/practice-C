#include <stdio.h>
#include <math.h>

int main(){
    int a;
    float b;
    char nama;

    char nama[5]= {'k', 'l', 'a', 's'} ;
    printf("huruf adalah %s", nama);

    printf("inputkan nilai a : ");

    /*scanf("%d", &a); 
    printf("inputkan nilai b: ");
    scanf("%f", &b); 
    printf ("nilai a adalah : %d \n", a);
    printf ("nilai a adalah : %.1f \n", b);
     ini hanya komentar tidak untuk di run
    
    */
    
}